import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faXmark } from "@fortawesome/free-solid-svg-icons";

const XMarkIcon = () => {
  return <FontAwesomeIcon icon={faXmark} style={{ color: "#000000" }} />;
};

export default XMarkIcon;
